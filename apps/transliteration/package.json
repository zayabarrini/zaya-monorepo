{
  "name": "transliteration",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --exec python3 api/index.py",
    "dev:watch": "nodemon --watch api --watch src --exec python3 api/index.py",
    "dev:watchdog": "python watch.py",
    "build": "echo 'Python build complete'",
    "vercel-build": "pip install -r requirements.txt"
  }
}